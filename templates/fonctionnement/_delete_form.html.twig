<form method="post" action="{{ path('app_fonctionnement_delete', {'id': fonctionnement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fonctionnement.id) }}">
    <button class="btn">Delete</button>
</form>
